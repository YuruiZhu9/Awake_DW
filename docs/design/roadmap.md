# 后续版本开发路线图

> 2026-09-08 建立；以 alpha12 为继承基线。这是工作优先级，不是未经确认的功能承诺。

## 已落实 · alpha12
- 猫咪常驻可点击提示；八主题均具备花边来源与主题粒子；布局延续主操作优先。
- 共享视觉组件位于 core:designsystem；主题存储键、业务与权限保持兼容。
- 实施记录：`docs/superpowers/plans/2026-09-08-v03-alpha12-visual-baseline-plan.md`。
- 发布证据：`docs/superpowers/checklists/v0.3-alpha12-visual-qa.md`。

## P0 · 下一轮先补设备验收
- 在实际 Android 设备验证 alpha11 → alpha12 同签名覆盖升级，保留记录、主题和提醒设置。
- 完整导航壳下检查 360dp、1.3×／1.5×／2× 字体、横屏、TalkBack 与减少动态设置。
- 正常／减少动态分别检查猫、粒子与页面切换；真实设备性能结论需记录设备、系统及操作步骤。
- 检查圣职浅色边饰、深夜与哥特在低亮度的阅读层次。
- 完成条件：逐项有记录；发现问题先修可达性与阅读性，不撤掉已确认的美术细节作为捷径。

## P1 · 基线稳定后的精修
- 为首页／统计／设置建立八主题、空态／有数据、长文本的稳定截图对照。
- 主题选择预览与真实页面装饰保持一致，评估小卡片可读性，不机械缩放全部花边。
- 审核背景绘制与装饰缓存开销；优先复用 Path／缓存，避免新增逐帧分配与全屏模糊。
- 完成条件：每轮只改变一个视觉变量，保留前后对照并跑全部受影响测试。

## P2 · 需用户确认后再排期
- 新主题、新素材、页面结构调整和额外交互；先写设计提案，不作为本轮默认任务。
- 不做养成、签到、奖励、收藏或与喝水无关的扩张。

## 0.4.1 · 细节精修（2026-09-08 用户确认）
- 计划：`docs/superpowers/plans/2026-09-08-v041-detail-refinement-plan.md`。
- 范围：统计时间线五条折叠、全主题缩略图/页面蕾丝策略统一、已有本地背景候选低频轮换、首页猫咪未回应留白饰、心意文案润色。
- 版本默认按 0.4.x 局部修复推进，不改变 0.4.0 的首页摘要等宽居中基线；业务、导航、权限与持久化键保持兼容。

## 每版本固定流程
1. 阅读 rules／AGENTS／视觉基线，写清范围和非目标。
2. 新增版本 plan；设计变更先取得确认，文档单独提交。
3. 小步实现、补回归测试，确认所有主题和减少动态不回退。
4. `gradlew.bat ktlintCheck build`，记录设备验收与未通过项；不把缓存命中当成新跑测次数。
5. 提升版本号，生成 Debug／Release，检查包名／版本／签名并生成校验文件。
6. 更新 CHANGELOG 和 QA，经用户要求提交推送；未经真机验收默认发布 alpha；用户指定无后缀版本时，明确待验收范围，不宣称真机稳定性通过。

## 0.4.0 阶段收口
- 本轮按用户指定编号 0.4.0，继承 alpha12 视觉基线，修复首页事实摘要等宽居中排版。
- 计划：`docs/superpowers/plans/2026-09-08-v040-home-summary-plan.md`。发布状态与证据：`docs/superpowers/checklists/v0.4.0-visual-qa.md`。
- 后续 0.4.x 优先处理设备验收与局部修复；0.5.0 需新的范围确认，不默认重做现有美术。

## 2026-09-10 · 首页主题背景修复
- 修复 `LolitaBackdrop` 根容器尺寸坍缩：背景绘制层必须占满页面，而不是只在主题缩略图中可见。
- 接入用户提供的晨雾蓝瓷、午后藕荷、黄昏奶茶、雾紫玫瑰专属主图；运行时保留压缩衍生图，原 PNG 不进入 APK。
- 黑哥特、白色圣职、薄荷巧克力继续使用既有专属画框；深夜青黛在新主图确认前继续使用现有回退。
- 自动化覆盖背景全尺寸布局、八主题素材映射/解码、首页首屏可达性与原生渲染诊断图；真机 OLED、横屏、大字体、减少动态和不同导航栏高度仍待验收。

## 0.5.0 · 文字反馈与纠错（2026-09-10 用户确认）
- 计划：`docs/superpowers/plans/2026-09-10-v050-ux-optimization-plan.md`；验证记录：`docs/superpowers/checklists/v0.5.0-ux-qa.md`。
- 范围：反馈三通道分离（环心确认 / 环下达标横幅 / 猫语气泡）、语料按用途分层（长句可编辑、短句内置）、
  防抖重复触发回显、记录撤回与删除、快捷量语义对齐、通知时段标题修正、问候不截断、隐藏手势说明与无障碍动作。
- 非目标：背景素材、主题色、蕾丝花边、粒子、猫的矢量形象、首页摘要等宽居中基线、导航、权限、持久化键名。
- 已于 2026-09-10 发布 **0.5.0 / versionCode 21**（Debug 与 Release 归档于 `dist/`）。真机验收仍未完成，已在版本 QA 顶部披露；按 rules §八.5，这不代表已达完整 Definition of Done。签名环境变量未注入，Release 变体回退 debug 证书，与 0.4.1 / 0.4.2 的既有状态一致。

## 0.5.1 · 打卡引文与同屏节奏（2026-09-11 用户确认）
- 计划：`docs/superpowers/plans/2026-09-11-v051-praise-quote-plan.md`；基线补充：`visual-baseline.md` §12。
- 范围：打卡确认语换成「古典引文 / 原创短句 + 可选落款」（带落款者取自公有领域诗文，逐条核对原文）；打卡引文与猫语**同屏同拍**、停留时长一并延到 3.0s；环心文案区固定高度零位移；猫语气泡降为次级层级。
- 非目标：背景素材、主题色、蕾丝花边、粒子、猫的矢量形象、108 句心意文案库、猫语池、达标横幅、导航、权限、持久化键名。
- 状态：已于 2026-09-15 发布 **0.5.1 / versionCode 22**（Debug 与 Release 归档于 `dist/`，提交 `a98c8b9`）；真机对比度与大字体折行待验收，Release 仍为 debug 证书（与 0.5.0 一致）。

## 0.5.x · 下一轮候选（需用户确认后再排期）
- 夜间 22:00–06:00 猫为安睡态，但「点击我试试~」入口仍常驻、摸猫仍回一句猫语，交互与状态存在落差；
  属「常驻提示」相关变更，按 rules §八.2 必须先取得用户确认，不以简化名义实施。
- 统计页仍只有今日 / 近七日 / 今日记录，周合计与达标天数未展示；属功能扩张，需新的范围确认。
- 去向（2026-09-15）：两项候选已并入 0.6 起精致化提案的轨道三，随 0.9.0 一并出方案；在此之前仍单独有效。

## 0.6.0 · 精致化地基（2026-09-15 立项，代码侧 2026-09-16 完成）

- 计划：`docs/superpowers/plans/2026-09-15-v060-refinement-foundation-plan.md`；语料草案：引文 v2（`2026-09-15-v060-praise-corpus-draft.md`）与文案库（`2026-09-16-v060-copies-corpus-draft.md`）均经使用者圈选后落码。
- 已完成：仓库清理（dist 514MB→52MB、死素材、cat 衍生图瘦身）、Robolectric 离线根治、深夜青黛专属主图、打卡引文换装现代白话 + 「作者《作品名》」完整出处、心意文案库 108 句第三轮重写；全部模块测试全绿。
- 待收口：真机补验清单回传（`docs/superpowers/checklists/v0.4-v0.5-device-acceptance-backlog.md`）→ 升 versionCode 23 出包。

## 0.7.1 · 场景收全三层 + 地平线演化（2026-09-16 发布，versionCode 24）

- 近景失焦层 `BokehLayer`：纯代码光斑（取主题粒子色族、贴边、中央避让、120s 漂移），八主题通用、无需素材——三层场景（背景 / 中景 / 近景）至此齐备。
- `SceneSpec.horizonShift` 第三锚点 + `GradientBackdrop` 光晕 / 洗染中心随时段偏移（晨上 / 昼中 / 夜沉）。
- 版本号纪律（使用者确认）：不再使用 alpha 后缀，发布即正式版本号递进（rules §八.5/§八.7 修订）；0.7.0-alpha1 已由使用者真机确认总体符合预期。

## 0.7.0 · 分层场景一阶（2026-09-16 立项并实施；同日以 **0.7.0-alpha1 / versionCode 23** 首发）

- 计划：`docs/superpowers/plans/2026-09-16-v070-layered-scene-plan.md`；基线：`visual-baseline.md` §13（moodboard「深夜信纸，会呼吸」经使用者确认）。
- 已实施：`MotionTokens` 动效 token、`SceneBackdropLayer` 中景层组件、粒子扩容（标准 80 / 安静 24）、`SceneSpec` 时段氛围插值、深夜青黛中景素材接入（其余主题静默不受影响）；designsystem 测试全绿。
- 发布：0.6.0 未独立发包，其全部内容随 **0.7.0-alpha1**（Release 4.8 MiB，签名与 0.4.1 一致可覆盖安装）合并首发；版本 QA 与待验收项：`docs/superpowers/checklists/v0.7.0-alpha1-qa.md`。
- 待验收：真机帧率与观感（正常 / 减少动态）；近景失焦层素材待使用者重生成后接入；渐变地平线偏移推迟 0.7.1。

## 0.6 起 · 精致化阶段总纲（2026-09-15 提案，同日经用户逐项确认）

- 提案全文：`docs/design/v0.6-refinement-charter.md`。0→1 已于 0.5.1 收口；1→10 按「三轨道 × 两地基」推进：
  地基 A 真机验收闭环（最高优先）、地基 B 治理与仓库清理（dist / 死素材 / 构建环境根治）；
  轨道一 内容策展化（0.6.0：引文库改散文语录制、深夜青黛补图）、
  轨道二 视觉场景化（0.7.0 分层视差 + 场景状态机；0.8.0 AGSL 氛围层，前置 alpha13 moodboard——已于 2026-09-16 确认）、
  轨道三 体验纵深（0.9.0：统计周维度、达标仪式、夜间猫）；1.0.0 以完整 Definition of Done 收口。
- D1–D10 已于 2026-09-15 逐项确认（记录见提案 §9.1）。
