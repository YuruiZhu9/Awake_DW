# v1.9.0 · 版本 QA（2026-09-22 发布）

- 包：`dist/Awake_DW-v1.9.0-release.apk`（versionCode 39，6,008,132 B ≈ 5.7 MiB，SHA-256 `a55b3c6e…07b3` 见同名 SUMS 文件）
- 签名：debug 证书 SHA-256 `25991b6c…d666`，与历史一致，可直接覆盖安装
- 自动化：全部模块单元测试与 `ktlintCheck build` 全绿

## 本版内容（动效系统一阶 + 液化回退）

1. **页签转场升级**：方向感知横向视差——去程新页自右轻入、回程自左轻入，位移 10dp（§10.2 ≤12dp 纪律不变），`MotionTokens.EasingStandard` 缓动 + 0.99 微缩放，240/170ms；替代原竖直「淡入 + 8dp 上移」。退场保持纯淡出（克制基调：只让新页动）。
2. **底栏选中弹性反馈**：选中图标 spring（damping 0.5 / stiffness Medium）弹入，静置在 1.08× 轻强调；取消选中回落 1.0×。
3. **环体液化回退**：`ProgressRing` 与 `MotionTokens` 恢复至 1.7.0 状态（衬线大数字 + 浮空环 + 珠扣），`ProgressRingLiquidTest` 移除。否决理由与教训见 roadmap「长程美学路线」。

## 真机闸门

- [ ] 页签左/右切换的方向感与顺度（是否真比上下淡入更顺）
- [ ] 转场在八主题与减少动态开启下的表现（减少动态应为纯淡入淡出、无位移）
- [ ] 底栏弹性反馈的分寸（1.08× 静置强调会不会太跳、spring 回弹会不会过头）
- [ ] 引导页 ↔ 设置页「白名单指引」路径的转场方向是否自然
