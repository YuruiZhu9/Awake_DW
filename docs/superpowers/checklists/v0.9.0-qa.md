# v0.9.0 · 版本 QA（2026-09-16 发布）

- 包：`dist/Awake_DW-v0.9.0-release.apk`（versionCode 26，5,045,513 B ≈ 4.8 MiB）
- 签名：debug 证书 SHA-256 `25991b6c…d666`，与历史一致，可直接覆盖安装
- 自动化：全部模块单元测试 + `ktlintCheck build` 全绿；新增 StatsMath 周摘要断言、CatHintTest 文案状态断言

## 本版新增（0.6 提案轨道三全部落地）

1. **统计近七日摘要**：「今日饮水」面板两格新事实——近七日合计（满 1L 以升显示）+ 近七日达标天数（N/7 天），与柱状图同源；事实陈述、无奖励语义。
2. **达标时刻光环**：当日首次达标时环心向外扩散柔光 + 细环（1.2s 单次），与缎带同拍；减少动态静默（缎带仍在）。D10 边界：仅视觉确认，无积分连击。
3. **夜间猫入口一致性**：22:00–06:00 安睡态入口文案「嘘，我在睡~」（入口仍常驻可点）；`catHintOf(mood)` 纯函数驱动。

## 待真机观察（非阻塞）

- [ ] 达标光环：克制度、与缎带同拍的观感；减少动态下光环静默
- [ ] 统计摘要：360dp 与 1.3×–2× 字体下两格折行；「0 ml」与升制的显示
- [ ] 夜间（22 点后）入口文案「嘘，我在睡~」观感；点击仍回应正常
