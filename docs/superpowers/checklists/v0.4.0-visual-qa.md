# 0.4.0 首页摘要与发布验收

## 版本（2026-09-08）
- 0.4.0，versionCode 18；按用户指定使用无 alpha 后缀编号，真机验收不因此自动完成。
- 原 FlowRow 改为满宽等权 Row，三项摘要栏内居中；无最近一杯时两栏等分，保留原纸面与花边。
- 长值允许自然换行与高度扩展，未修改饮水业务、权限、主题存储键或签名配置。

## 已完成自动化与产物校验
- [x] 专项 `gradlew.bat :feature:home:testDebugUnitTest :core:designsystem:testDebugUnitTest ktlintCheck`：BUILD SUCCESSFUL in 1m 10s，177 tasks（14 executed，163 up-to-date）。
- [x] 新增 BadgesRowLayoutTest：三栏等宽／两端对称、空态两栏、320dp／2× 字体下长值栏目边界不交叠、八主题诊断图。
- [x] 完整 `gradlew.bat ktlintCheck build`：BUILD SUCCESSFUL in 2m 1s，1188 tasks（87 executed，1101 up-to-date）。
- [x] 101 份 JUnit XML 报告汇总 499 个用例，0 failures、0 errors、0 skipped；包含 Debug／Release 及缓存报告，不代表所有用例本轮重跑。
- [x] 八主题摘要诊断图人工复核，三列等宽、两端留白对称；路径 `feature/home/build/reports/visual-review/<testVariant>/v040-summary-*.png`。拼图 `build/v040-summary-contact.png`；均为本地诊断产物，不是设备截图。
- [x] 独立摘要诊断图没有全屏主题底图，尤其深夜主题的透明纸面不能据此判定最终页面对比度；本轮不改颜色，实际背景下观感继续列入真机验收。
- [x] 包内版本均为 0.4.0 / 18；Debug 包名 com.awakedw.app.debug，Release 包名 com.awakedw.app。
- [x] 两个 APK 均通过 apksigner verify，证书 SHA-256 与 alpha12 同变体一致，覆盖升级实际操作仍待设备确认。
- [x] APK 已复制 dist；保留 alpha12 历史包；生成 `dist/Awake_DW-v0.4.0-SHA256SUMS.txt`。
- [x] `git diff --check` 通过。日志：`build/v040-targeted.log`、`build/v040-release-build.log`（不入库）。

## 安装包 SHA-256
- `Awake_DW-v0.4.0-debug.apk`：22355128 字节
  - `51E7D07BE9D15038C418947A9F4209C0E46F0EC6C43C4920A9B912423E275576`
- `Awake_DW-v0.4.0-release.apk`：6506058 字节
  - `51BFEAF68CEA246BAAA61B0402B8E83D234F7543538CAA1BA24D29D5F8EE9E8E`

## 提交与远端发布
- 文档／实现／发布按独立提交组织；用户已授权推送至现有 GitHub `YuruiZhu9/Awake_DW` 的 main 分支。
- 推送结果以 Git 返回与远端提交核对为准；不创建额外 GitHub Release，不改写历史。

## 真机待验收
- [ ] 小屏与系统导航壳下首页首屏、滚动、1.5×／2× 字体及横屏。
- [ ] 覆盖升级后记录、主题、提醒设置保留；后台提醒正常。
- [ ] 正常与减少动态模式、OLED 低亮度下的主题观感。

版本号由用户指定，不等同于真机稳定性验收完成。
