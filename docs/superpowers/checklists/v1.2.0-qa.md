# v1.2.0 · 版本 QA（2026-09-17 发布）

- 包：`dist/Awake_DW-v1.2.0-release.apk`（versionCode 31）
- 签名：debug 证书 SHA-256 `25991b6c…d666`，与 1.1.0 一致，可直接覆盖安装
- 自动化：全部模块单元测试 + `ktlintCheck build` 全绿；统计页 populated / target-met / empty 截图重拍复查

> 已知偶发（非本版引入）：`ThemePreviewVisualTest`（settings，release 变体）在本轮全量中出现过一次 `CalledFromWrongThreadException`——0.4.2 记录过的「异步图片装载时序」同族 flake，两次重跑（含 `--rerun-tasks` 强制重跑）均绿。留观，不阻塞。

## 本版内容（视觉 refresh 二阶：统计页手账化）

1. **手账数据对页**：「今日饮水」+「近七日」同纸共框（`LetterSheet`）——总量数字 displaySmall 版面主角、细轨珍珠进度、达标「已达成」右置、发丝 2×2 事实表格、210dp 周图。
2. **周图主角化**：绘图区 160 → 210dp；柱身水墨渐染（自上而下沉墨）替代平涂色块。
3. **记录页**：今日时间线单独成一张纸；`PaperSectionTitle` 公共分节组件供纸内分节（PaperPanel 内部同源复用）。
4. 业务、删除流程、语义键、0.9.2 全部成果零改动。

## 待真机观察（非阻塞）

- [ ] 210dp 周图在 360dp 窄屏的柱宽与留白观感
- [ ] 水墨渐染柱在暗色主题（深夜/哥特）的层次
- [ ] 两张手账纸（数据对页 + 记录页）的滚动翻页节奏
