# alpha11 三页视觉与交互验收

## 设计落实
- 晨雾蓝瓷替换旧清晨薄荷视觉，EMERALD/FIXED_EMERALD 键保持兼容，薄巧不变。
- 圣职原图未改；衍生图增强银灰轮廓，正常绘画叠加及独立中心遮罩使边缘可见。
- 猫右上提示与摸猫同动作；会话内交互后隐藏，回应左对齐、长文本自适应。
- 统计页：总量、细进度、事实摘要、可选七日图表、记录列表、空态；安静粒子。
- 我的页：统一页头与分组、48dp 步进器、细轨道圆手柄、主题底部选择面板、声音合组。

## 自动化
- 图表错峰终态测试先失败（缺少 columnGrowth），实现后各列终态均为 1；选择日期显示真实值。
- 新蓝瓷纯色按钮及正文/副文字对背景端点对比度至少 4.5:1；图片叠加后的全像素对比度不在此断言范围内。
- 猫提示点击一次调用一次，回应消失后提示不反复出现；长句所有行左边界一致、猫/按钮无交叠。
- 统计页 360dp：真实数据和空数据渲染，大字体 1.5 倍时图表选择和记录仍可达。
- 我的页 360dp / 1.3 倍字体：主题网格不再常驻，选择面板可滚动，选择蓝瓷写回设置，关闭后返回原页。
- 首页 360dp / 1.3 倍字体：猫与主按钮首屏可见。三页原生渲染已逐一检查，新增测试资产只在测试构建使用。
- 输出：各 feature 的 `build/reports/visual-review/<testVariant>/alpha11-*.png`。
- 最终 `ktlintCheck build`：2026-09-08 通过，最后一轮 51s / 1188 tasks（30 executed）。
- 测试报告合计 **489 次用例执行，0 failures / 0 skipped**（含 Debug/Release 重复变体）。
- 完整导航冷启动测试同步新统计语义「记录次数 0 次」，保持原有页签切换/首页返回退出断言并通过。
- `aapt dump badging`：com.awakedw.app，versionCode 16，versionName 0.3.0-alpha11。
- `apksigner verify`：release 通过，保持项目现有签名配置（无正式签名环境时回退 debug 签名）。
- Debug/Release 包已复制 dist，分别 22355124 / 6522454 字节；`git diff --check` 通过。

### SHA-256
- Debug：`CCDED029BA48B3EC25C71973E5BF24F27D23E68EF021E1608F56CA681033FBF1`
- Release：`19EF5350D9850D7CD0F6C9D29E11F1986AAC119B6A77DA94BAECE92D00BA02DC`

## 仍待真机观察（未验收）
- [ ] 中端设备正常/减少动态模式下粒子、猫与页面切换的帧率。
- [ ] 状态栏＋底部导航壳下的小屏首屏位置、超大字体/横屏和键盘弹出。
- [ ] 圣职背景在 OLED 低亮度、不同色温设备上的实际层次。
- [ ] 蓝瓷升级后保持原主题选择；后台提醒和覆盖安装实际表现。

当前 adb 无连接设备。原生功能页诊断图不等于真机截图或整机性能验收。本轮无需新素材。
