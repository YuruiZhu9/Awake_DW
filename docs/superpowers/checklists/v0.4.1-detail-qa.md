# 0.4.1 detail refinement QA

## Release

- Date: 2026-09-09
- Version: 0.4.1, versionCode 19
- Scope: statistics timeline folding, shared theme lace in previews and pages, local same-theme background rotation, home mascot empty-state ornament, and care-copy refinement.
- Non-goals: no changes to water-record calculations, debounce behavior, reminders, navigation, permissions, theme storage keys, network access, or copy-library JSON format.

## Automated verification

- [x] `:feature:stats:testDebugUnitTest`: timeline shows all records through five, keeps the latest five when collapsed, expands and collapses with the accessible action label.
- [x] `:core:designsystem:testDebugUnitTest`: all theme artwork candidates are packaged, decodable through the existing asset path, under the existing size budget, and cycle order is stable.
- [x] `:feature:settings:testDebugUnitTest`: theme choice interaction and artwork preview regressions pass; current-theme entry uses the same production artwork/lace components.
- [x] `:feature:home:testDebugUnitTest`: persistent mascot hint, mascot tap callback, long-response layout and first-viewport action coverage remain green.
- [x] `ktlintCheck`: full project formatting check passes.
- [x] `ktlintCheck build`: BUILD SUCCESSFUL in 4m 26s; 1188 actionable tasks, 162 executed, 1026 up-to-date.

## Asset verification

- [x] Existing user originals remain unchanged.
- [x] Candidate generation is reproducible with `tools/prepare-theme-candidates.ps1`; `tools/prepare-theme-art.ps1` invokes it after the existing dedicated-art step.
- [x] Six additional local JPEG candidates are 810x1440 and below 350KB each.
- [x] No candidate is assigned to Gothic, Cleric or Thin Mint; their dedicated frames remain the only frame source.
- [x] Normal motion rotates candidates every 12 seconds with a crossfade; reduced motion does not start the rotation loop.

## Visual/manual acceptance still required

- [ ] 360dp and 1.3x/1.5x/2x text: statistics folded/unfolded actions remain reachable and do not cover the bottom navigation.
- [ ] All eight themes: preview cards and full pages show the expected local image/lace treatment; dedicated frames are not double-framed.
- [ ] Home empty mascot rail looks balanced without competing with the progress ring, persistent hint or record action.
- [ ] Normal/reduced motion on a mid-range Android device: no visible flash, jump, or frame-rate regression during a 12-second rotation.
- [ ] Landscape, OLED low brightness, TalkBack, and upgrade-over-install preserve the existing records, settings, reminder state and signing identity.

## Release artifacts

- [x] Debug APK: `app/build/outputs/apk/debug/app-debug.apk`; package `com.awakedw.app.debug`, version `0.4.1` / code `19`; `apksigner verify` passed.
- [x] Release APK: `app/build/outputs/apk/release/app-release.apk`; package `com.awakedw.app`, version `0.4.1` / code `19`; `apksigner verify` passed.
- [x] Both variants use the existing Android Debug certificate (SHA-256 `25991b6c80a27b9d07f4e31cb1d2ae605c7aeb533121a1ba0534d5883fa8d666`); signing configuration was not changed.
- [x] Package dump confirms release `com.awakedw.app`, versionCode `19`, versionName `0.4.1`, minSdk `26`, targetSdk `35`.
- [x] Candidate APK SHA-256 recorded below.

### SHA-256

- Debug `app-debug.apk`: `51F3884EAFEE3B559584488A2BA4895AC4AD5642EF6B96C694A66E739A5DEA4B`
- Release `app-release.apk`: `EAAE4C6585017E3D504575A5D4981D5B40AD62450B432F6B8B82A01546EF49AA`
