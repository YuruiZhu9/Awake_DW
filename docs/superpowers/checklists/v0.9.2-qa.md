# v0.9.2 · 版本 QA（2026-09-17 发布）

- 包：`dist/Awake_DW-v0.9.2-release.apk`（versionCode 28）
- 签名：debug 证书 SHA-256 `25991b6c…d666`，与 0.9.1 一致，可直接覆盖安装
- 自动化：全部模块单元测试 + `ktlintCheck build` 全绿；`StatsMathTest` 新增 `dayReadout` / `todayProgressLabel` 断言；`StatsVisualReviewTest` 读数断言随新格式更新，populated / target-met / empty / large-font 四场景截图重拍并人工复查

## 本版内容（统计页美观性抛光，方案见 `docs/superpowers/plans/2026-09-17-v092-stats-visual-polish-plan.md`）

1. **「今日饮水」面板重排**：三块面板统一 `PanelTitle` 饰线轨；自定义 3dp 细轨 + 主色填充 + 进度头珍珠替代 M3 进度条（其自带终点圆点在空态也悬着，读起来像滑块/假满）；达标「已达成」右置主色位（D10：仍只是文字确认，无徽章奖励语义）。
2. **事实四格等宽居中 + 发丝分隔**：与首页摘要等宽居中规则（0.4.0）同族；`StatsFact` 合并语义「标签 值」不变，`StatsWeekSummaryLayoutTest`（0.9.1）原断言保持绿。
3. **图表面板读数一行化**：「9月8日 · 1250ml」+「┄ 目标 1600ml」图例替代两行说明书散文；柱语义与读数转友好日期（`StatsMath.dayReadout`）；空态「暂无饮水数据」居中。
4. 业务、持久化键、导航、权限、删除流程、时间线行结构、柱图几何与生长动画零改动。

## 待真机观察（非阻塞）

- [ ] 细轨进度与珍珠在八主题（尤其深夜青黛/黑哥特暗底）下的对比度
- [ ] 发丝分隔与四格居中在 1.5×–2× 字体下的折行观感
- [ ] 图例虚线小样的可读性；选中列读数「N月N日 ·Xml」折行是否自然
