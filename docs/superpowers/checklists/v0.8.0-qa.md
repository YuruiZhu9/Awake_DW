# v0.8.0 · 版本 QA（2026-09-16 发布）

- 包：`dist/Awake_DW-v0.8.0-release.apk`（versionCode 25，约 4.8 MiB）
- 签名：debug 证书 SHA-256 `25991b6c…d666`，与历史一致，可直接覆盖安装
- 自动化：全部模块单元测试 + `ktlintCheck build` 全绿（含 lint NewApi 守卫检查、AgslGateTest 门控矩阵）

## 本版新增

- **AGSL 雾光层**（`AgslMistLayer`，alpha13 §13.4）：全屏动态雾光着色器，挂载于 `GradientBackdrop`，九处页面底座一处生效。
- **三重门控**：Android 13+（API 33）/ 非减少动态 / 着色器构造成功；任一不满足整层静默，观感与 0.7.1 完全一致。

## 待真机观察（非阻塞；需 Android 13+ 设备）

- [ ] 雾光流动克制不喧宾（最高约 12% 透明度、中央阅读列弱于边缘）
- [ ] GPU 负载可接受：滑动 / 打卡无掉帧；减少动态下雾光静止
- [ ] 八主题色雾观感（halo 色随主题变化）；Android 12 及以下与 0.7.1 无差异
- 日常使用反馈即可，不专项走查。
