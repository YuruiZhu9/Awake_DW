# v1.3.0 · 版本 QA（2026-09-17 发布）

- 包：`dist/Awake_DW-v1.3.0-release.apk`（versionCode 32）
- 签名：debug 证书 SHA-256 `25991b6c…d666`，与 1.2.0 一致，可直接覆盖安装
- 自动化：全部模块单元测试 + `ktlintCheck build` 全绿；新增 `InkWashTest`（组合 + 真实触摸路径冒烟）；首页八主题、统计三态、设置主题面板截图重拍复查

## 本版内容（视觉 refresh 三阶：全局墨水语言）

1. **墨水浸润 `Modifier.inkWash`**：按住晕开（180ms）、松手收干（260ms）、峰值 12% 透明度。接入范围（自有胶囊/卡片级）：快捷饮量胶囊、猫入口气泡、设置主题卡、提醒间隔 chip；行级组件与 M3 组件（TextButton、底栏）保留默认反馈。
2. **双纸面板**：`PaperPanel` 内嵌一道阳文细框（lace 22%），与印章内框同语言；`LetterSheet` 保持边距线语言不叠加内框。
3. 主题卡/间隔 chip 从 M3 `Surface(onClick)` 重构为 Box + selectable + 墨水浸润；选中态、Role.RadioButton 语义、48dp 触控下限全部保持；设置既有交互测试（选中持久化、主题选择）全绿。
4. 业务、持久化键、导航、权限、文案零改动。

## 待真机观察（非阻塞）

- [ ] 墨水浸润的洇开节奏与克制度（对比原涟漪是否更「纸墨」）
- [ ] 双纸内框在八主题的可见度（设计为低存在感，暗色主题尤其留意）
- [ ] 暗色主题选中 chip 上白墨按压的可读性
- [ ] 主题卡重构后实机点选的手感与即时生效（自动化已守）
