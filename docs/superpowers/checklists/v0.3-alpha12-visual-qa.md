# alpha12 视觉迭代与发布验收

发布日期：2026-09-08；版本 `0.3.0-alpha12`，versionCode `17`。这是 alpha 迭代版，未宣称真机验收完成。

## 实施与长期指导
- 猫咪常驻气泡：重复点击调用 onPet，猫本体共用回调，回应中和回应消失后仍可见。
- 普通五主题使用主题色代码花边；哥特／圣职／薄巧保留专属图框，禁止重复叠框。
- 八主题粒子按珍珠／银芒／花瓣区分，中央阅读区减弱，沿用减少动态机制。
- 根 AGENTS.md、rules.md、`docs/design/visual-baseline.md` 与 `docs/design/roadmap.md` 为后续版本的固定阅读入口；历史隐藏猫提示规则已被常驻要求覆盖。

## 自动化与产物验证
- 专项：首页／设计系统 Debug 测试通过；常驻提示、长回应布局、花边来源和粒子映射有回归覆盖。
- 完整命令：`gradlew.bat ktlintCheck build --console=plain`；2026-09-08 结果 `BUILD SUCCESSFUL in 4m 7s`，1188 tasks（207 executed，981 up-to-date）。
- 本地 99 个 JUnit XML 报告共 491 个用例，0 failures、0 errors、0 skipped；包含 Debug／Release 变体及缓存报告，不代表全部在本轮重新执行。
- 构建日志中的 `incomplete input` 来自损坏图片解码测试；最终 Gradle 构建成功，无失败用例。PowerShell 将原生 stderr 格式化为错误记录，不能单凭该记录判断构建失败。
- 八主题首页 360dp／1.3× 字体原生渲染测试通过，气泡／猫／主操作可见；产物在 `feature/home/build/reports/visual-review/<testVariant>/alpha12-*.png`。截图生成与自动布局断言不等于八主题真机视觉验收。
- `aapt dump badging`：Debug 为 `com.awakedw.app.debug`，Release 为 `com.awakedw.app`；均为 versionCode 17、versionName 0.3.0-alpha12。
- Debug／Release 均通过 `apksigner verify`，各自证书 SHA-256 与 alpha11 同变体一致。签名配置未修改；实际覆盖安装保留数据仍待设备验证。
- 安装包位于 `dist/`；原 alpha11 包保留，未覆盖。校验文件 `dist/Awake_DW-v0.3.0-alpha12-SHA256SUMS.txt`。
- `git diff --check` 通过；本地日志路径 `build/alpha12-release-build.log`（不入库）。

## 安装包 SHA-256
- `Awake_DW-v0.3.0-alpha12-debug.apk`：22355124 字节
  - `7E4788F2A2F634E1444BC7D08E369D9DE2815E09860233C2DCE0DB52ADA91103`
- `Awake_DW-v0.3.0-alpha12-release.apk`：6522454 字节
  - `27C907A95A98C2ECEA25F3621F82DAEE967A58681FDE2FA11C71691413C7F39C`

## 仍待真机观察（未验收）
- [ ] alpha11 → alpha12 同变体覆盖安装，记录、主题、提醒设置保留。
- [ ] 中端设备正常／减少动态模式下猫、粒子与页面切换帧率。
- [ ] 完整系统栏／导航壳下的小屏、1.5×／2× 字体、横屏、TalkBack。
- [ ] 圣职／深色主题在 OLED 低亮度和不同色温设备的阅读层次。
- [ ] 后台提醒与升级后的实际行为。

未安装到实际设备；不把自动化与签名核对等同于真机覆盖升级成功。
