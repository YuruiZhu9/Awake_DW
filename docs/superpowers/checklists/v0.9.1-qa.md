# v0.9.1 · 版本 QA（2026-09-17 发布）

- 包：`dist/Awake_DW-v0.9.1-release.apk`（versionCode 27，5,045,513 B ≈ 4.8 MiB）
- 签名：debug 证书 SHA-256 `25991b6c…d666`，与 0.9.0 一致，可直接覆盖安装
- 自动化：全部模块单元测试 + `ktlintCheck build` 全绿；新增 StatsWeekSummaryLayoutTest（周摘要窄屏大字体）与 RingNoteRenderTest 大字档长落款用例

## 本版内容（0.9.x 细节抛光轮）

1. **动效 token 收敛**：`MotionTokens` 扩展为场景与氛围动效唯一出处——主图横切 900ms、主图 12s 轮换、背景光晕 8s 呼吸、漂浮粒子 48s 循环、近景光斑 120s 循环、猫立绘/光袋共用 1.5s 呼吸单程全部入表；`LolitaBackdrop` 一处内联 `700` 魔法数收敛为 `DURATION_SCENE_REVEAL_MS`。纯常量搬家与命名收敛，**观感零变化**。
2. **大字体布局回归补强**：统计近七日摘要（此前视觉 review 测试手搓 state 从未携带 week 字段，摘要行取值与展示无任何断言）补 360dp + fontScale 2.0 真实推导值（「7.2 L」「3/7 天」）断言；环心引文补 fontScale 2.0 下两行正文 + 14 字上限长落款完整展示断言。

## 待真机观察（非阻塞）

- [ ] 本轮无观感变化（常量搬家 + 测试补强），无需专项走查；随日常使用按 `v1.0-acceptance.md` 持续走查即可
