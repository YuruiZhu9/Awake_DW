# 后续设计与开发指导

后续开发必须先阅读：`rules.md`、`docs/design/visual-baseline.md`、`docs/design/roadmap.md`、对应规格与 plan。

当前设计基线为 alpha12 + alpha13 场景化增补（`visual-baseline.md` §13/§14，「深夜信纸，会呼吸」），并继承用户确认的 0.4.0 首页摘要等宽居中规则、0.5.0 的文字反馈三通道与语料分层规则（§11）、0.5.1/0.6.0 的引文与语料纪律（§12/§14）。发生冲突时，用户新确认的需求优先，业务遵循历史功能规格，视觉遵循当前基线；旧版本隐藏猫提示等规则仅作历史记录。

关于文字反馈的现状，动手前先看清分工：心意文案库（108 句，0.6.0 第三轮重写为现代白话）**只**用于首页问候语与提醒通知正文，仍可整库编辑；打卡确认与猫咪回应走内置短句池（`core/data/.../copy/ShortCopies.kt`），不随库变化。打卡确认在环心、达标横幅在环下、猫语在气泡，三者不得再合并到同一块位置。

## Do
- 延续主题色、纸面质感、轻蕾丝和克制粒子。
- 先复用 `core:designsystem`，再在页面组装。
- 先写设计决策与验收条件，再实施代码。
- 视觉与交互改动必须附自动化覆盖，并留下真机待验收项。
- 改文案前先读 `CopyLibraryRepositoryTest` 与 `ShortCopiesTest`：长度、唯一性、无 `|`、无第二/三人称、同组起手不近似，都由测试守着，不是建议。
- **场景化纪律（alpha13 起）**：新增装饰组件一律走既有模式——运动学纯函数（`(seed, index)` 确定性推导）、只进 draw 相不触发重组、时段氛围经 `rememberSceneSpec()` 插值、减少动态下静止、条件不满足时整层静默（参照 `SceneBackdropLayer` / `BokehLayer` / `AgslMistLayer`）。
- **语料纪律（0.6.0 起）**：零文言；引文只取公有领域并逐条核对，落款为「作者《作品名》」完整出处；原创句不伪造出处。

## Don't
- 不隐藏常驻猫咪提示，不把它改成一次性 onboarding。
- 不只给少数主题做花边；不叠加重复图框。
- 不用高密度闪烁、随机粒子、强阴影或装饰遮挡主要操作。
- 不借视觉优化改动饮水业务、持久化键、导航或权限。
- 不用新素材替代现有艺术方向，除非先更新 moodboard/spec。
- 不把「打卡确认 / 达标横幅 / 猫语」重新塞回同一格，也不让心意文案库反过来影响猫语与打卡确认。
- 不给长文本重新加 `maxLines` 截断；不做没有确认步骤的删除。
- 不在装饰里做 3D 引擎、网络资源、高斯模糊滤镜或任何积分/连击/奖励语义（D10：达标仪式只做视觉确认）。
