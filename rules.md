# Awake_DW · Vibe Coding 开发规则

> 本文件对项目的所有开发环节有约束力。业务边界以历史功能规格为准；当前视觉与交互以 `docs/design/visual-baseline.md` 为准，历史视觉规格作为背景参考。alpha12 明确覆盖 alpha11 的“一次交互后隐藏猫咪提示”规则，现为常驻。后续规划入口为 `docs/design/roadmap.md`。需要改变基线时，先记录用户确认并修改相应规则／spec／plan，再实施；不能用历史 plan 回退已确认样式。

## 一、定位与克制

1. 这是**面向所有使用者的喝水应用**：全部文案以第一人称「我」的自述口吻书写（不管谁装在手机上，都是「我」在自己用），不出现「你和她 / 你们俩」式的两人称谓。每个决策先问：这让使用者的体验更优雅、更自然、更不打扰吗？
2. 审美基调：符合大众审美的高级感——克制、留白、呼吸感；动效与文案宁少毋滥，不堆砌。视觉装饰只服务喝水记录与提醒，不演化为收藏、养成或游戏机制。
3. 范围纪律：§8（明确不做）里的东西一律不做。出现"顺手加个 X"的冲动时，记入二期备忘，不写进本期代码。
4. 迭代优于完美：每一阶段结束时仓库里必须有一个能跑的最新版本——先达意，再打磨。

## 二、可迭代 · 可优化 · 可扩展（最高优先级）

1. **小步提交**：一次提交只做一件事；提交信息用 `<type>: <中文摘要>`（feat/fix/refactor/test/docs/chore）。禁止巨型混合提交。
2. **重构随行**：出现 hack、或同一逻辑重复到第三处时，当场收敛为共享实现，不留"下次再说"。
3. **扩展守则**：新功能必须长在正确的模块里——界面进 `:feature:*`，知识与能力沉淀进 `:core:*`；一件公共能力一旦出现第二个使用方，立即下沉 core。
4. **接口先行**：跨模块调用一律面向接口；不允许为了图快在 feature 里直连 Room/DataStore。

## 三、叙事纪律（对外含蓄原则）

1. README 只保留：项目名、一句简介（「一个用于提醒喝水的应用」）、技术栈、构建方式。**不展开罗列具体功能细节**（主题系统、统计页、文案库等均不描述），不放主界面截图，不做刻意的悬念式表达。
2. 项目名的含义不在任何公开文档中解释。
3. 隐私即设计：无网络权限、无第三方统计 SDK、日志不输出任何使用记录或个性化内容。
4. 建议仓库保持私有可见性。

## 四、架构铁律（详见 design.md §5.2）

1. 依赖方向恒定 `ui → domain ← data`；`:core:domain` 是纯 Kotlin，禁止 import Android 类。
2. `:feature:*` 之间互不依赖，跨页跳转经 `:app` 导航表。
3. 数据访问只经 Repository 接口；Room/DataStore 细节不出 `:core:data`。
4. UI 状态一律是不可变数据类；副作用只存在于 ViewModel 与 UseCase。

## 五、质量门槛（Definition of Done）

每一步收工前逐条过，缺一不算完成：

1. `./gradlew build` 全绿；
2. 新增或变更的 domain 行为有对应测试（data 层按风险补充）；
3. 涉及动画/视觉的改动在中端真机目测流畅不掉帧；
4. 工作区干净：不留 TODO/FIXME 散件、不留注释掉的死代码；
5. 需求发生变化时，先修改 design.md 并单独提交，再改代码——文档与代码永不失同步。

## 六、测试风格

1. domain 用例 TDD：先写失败的测试再实现；时间相关逻辑统一注入虚拟时钟。
2. 测试名用业务语言表达意图：如「未达成目标时不进入庆祝态」，拒绝 `testCalc_01` 式命名。

## 七、AI 协作守则

1. 每轮会话结束时仓库必须处于可构建、可运行状态。
2. 大改动先给一句话方案征得同意，不偷跑。
3. 设计未覆盖的决策点，凭"更温柔 / 更克制"原则判断，并在回复中显式标注所做的假设，供人复核。

## 八、视觉基线与版本发布

1. 每轮修改前阅读根 `AGENTS.md`、`docs/design/visual-baseline.md`、`docs/design/roadmap.md` 与对应版本 plan；后续版本默认继承 alpha12 视觉基线。
2. 任何移除花边、隐藏常驻提示、替换主题主色／素材、改变主操作层级的工作，均视为设计变更，先取得用户确认，不得以“重构／简化”为由实施。
3. 普通小屏保持主操作优先；超大字体允许滚动，但不截断文字、不覆盖控件。装饰低对比，正文与操作必须保持可读对比度。
4. 发布同步 versionCode/versionName、CHANGELOG、版本 QA、Debug/Release APK 与 SHA-256；验证包内版本及签名与上一版一致性，不改签名配置绕过安装问题。
5. 构建、测试与真机视觉验收分开记录。未完成真机验收时默认发布 alpha 包；用户可明确指定 0.4.0 等无后缀版本号，但必须在发布 QA 披露待验收项，不能仅凭版本号宣称已达到完整 Definition of Done 或稳定版质量。
6. 按文档、实现、发布拆分提交；仅在用户要求时提交／推送，不擅自强推或覆盖远端历史。README 继续保持含蓄，不加入功能展示。
7. 版本阶段按交付范围收口，不无限累加 alpha 序号。用户已指定本轮为 0.4.0（versionCode 18）；之后局部修复默认进入 0.4.x，新阶段再规划 0.5.0。版本号与验收证据分开管理。
