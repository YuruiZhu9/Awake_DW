package com.awakedw.core.designsystem

import androidx.compose.ui.unit.dp

/**
 * 页级统一水平留白（P3-1）：统计 / 设置 / 画廊三页共用同一常量，消除各自私有效值漂移。
 * 首页 28dp 为 hero 口径（进度环承重布局）刻意保留，不并入本常量。
 */
val PagePadding = 24.dp
